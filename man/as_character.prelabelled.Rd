% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prelabel.R
\name{as_character.prelabelled}
\alias{as_character.prelabelled}
\title{Coerce prelabelled vectors to semantic character workspace}
\usage{
\method{as_character}{prelabelled}(x, ...)
}
\arguments{
\item{x}{A \code{prelabelled} vector.}

\item{...}{Unused.}
}
\value{
A character vector where:
\itemize{
\item values are derived from the semantic labels;
\item original observations are preserved as attributes;
\item refinement metadata is retained;
\item the \code{"prelabelled"} class is removed.
}
}
\description{
Convert a \code{prelabelled} vector into a semantic character
workspace suitable for iterative refinement and inference
workflows.
}
\details{
Unlike base \code{\link[=as.character]{as.character()}}, this method preserves:
\itemize{
\item original observational values;
\item semantic label mappings;
\item refinement metadata;
\item additional custom attributes.
}

The method operationalises provisional semantic labels into
working character values while retaining the original observed
vector for reversibility and provenance-aware workflows.

This allows workflows such as:
\itemize{
\item \code{prelabel() \%>\% as.character() \%>\% refine()}
\item \code{prelabel() \%>\% as.character() \%>\% infer()}
\item iterative semantic harmonisation;
\item contextual semantic derivation.
}

The original observational values are preserved in the
\code{"original_values"} attribute.

The method intentionally separates:
\itemize{
\item observational evidence;
\item semantic operationalisation.
}

This distinction is important for provenance-aware semantic
refinement workflows where semantic interpretations may evolve
while original observations remain stable.

The coercion is therefore semantically reversible.
}
\examples{

x <- prelabel(
  c(
    "r",
    "pdf",
    "qmd"
  ),
  labels = c(
    r = "software development",
    pdf = "publication",
    qmd = "documentation"
  )
)

x

y <- as_character(x)

y

attributes(y)

attr(
  y,
  "original_values"
)

}
