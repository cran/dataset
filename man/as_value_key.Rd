% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_value_key.R
\name{as_value_key}
\alias{as_value_key}
\alias{invert_value_key}
\title{Coerce semantic mappings to canonical key-value form}
\usage{
as_value_key(x)

invert_value_key(x)
}
\arguments{
\item{x}{A semantic mapping carrier:
\itemize{
\item named character vector;
\item named list;
\item two-column data frame or tibble.
}}
}
\value{
A named character vector representing canonical semantic
key-value mappings.
}
\description{
Convert semantic mapping carriers into a canonical
named character vector representation.

Convert semantic mappings into a two-column relational form
suitable for joins, inspection, and roundtrip conversion with
\code{\link[=as_value_key]{as_value_key()}}.
}
\details{
\code{as_value_key()} standardises lightweight semantic mappings
supplied as:
\itemize{
\item named vectors;
\item named lists;
\item two-column data frames or tibbles.
}

The resulting object is suitable for:
\itemize{
\item \code{\link[=prelabel]{prelabel()}};
\item semantic grouping;
\item contextual reconstruction;
\item relational projection workflows.
}

Named lists may contain one-to-many mappings:

\preformatted{
list(
  conceptualisation = c(
    "D:/_package/alpha",
    "D:/_markdown/alpha-methodology"
  ),
  betaR = c(
    "D:/_packages/beta",
    "D:/_packages/prebeta"
  )
)
}

where multiple values share the same semantic key.

The function is intentionally lightweight. It does not:
\itemize{
\item validate ontologies;
\item enforce uniqueness;
\item resolve semantic conflicts;
\item construct graph objects;
\item perform recursive inheritance logic.
}
}
\examples{

# named vector
as_value_key(
  c(
    R = "functional_programming",
    png = "visualisation"
  )
)

# named list
as_value_key(
  list(
    R = "functional_programming",
    png = "visualisation"
  )
)

# tibble
mapping_tbl <- tibble::tibble(
  extension = c("R", "png"),
  activity = c(
    "functional_programming",
    "visualisation"
  )
)

as_value_key(mapping_tbl)

}
