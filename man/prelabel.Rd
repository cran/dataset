% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prelabel.R
\name{prelabel}
\alias{prelabel}
\alias{is.prelabelled}
\title{Add lightweight semantic mappings to a vector}
\usage{
prelabel(x, labels, unmatched = "keep", missing_label = "<NA>")

is.prelabelled(x)
}
\arguments{
\item{x}{A vector.}

\item{labels}{Candidate semantic mappings describing
provisional semantic assertions.

\code{labels} is internally normalised with \code{\link[=as_value_key]{as_value_key()}} and
may therefore be supplied as:
\itemize{
\item a named vector;
\item a named list;
\item a two-column data frame or tibble.
}}

\item{unmatched}{Behaviour for unmatched observational values.

One of:
\itemize{
\item \code{"keep"} (default): preserve unmatched values as
self-describing semantic assertions;
\item \code{"na"}: operationalise unmatched values as \code{NA} during
semantic coercion.
}}

\item{missing_label}{Semantic assertion used internally for
missing observational values.}
}
\value{
A vector with:
\itemize{
\item class \code{"prelabelled"};
\item attached provisional semantic vocabulary stored in the
\code{"prelabel"} attribute.
}
}
\description{
\code{prelabel()} applies lightweight semantic mappings to a vector
before formal definition with \code{\link[=defined]{defined()}}.
}
\details{
The \code{prelabelled} class is intended for:
\itemize{
\item provisional harmonisation;
\item contextual grouping;
\item lightweight classification;
\item semantic preprocessing workflows.
}

Unlike \code{\link[=defined]{defined()}}, \code{prelabel()} does not enforce
formal semantic definitions, namespaces, or units.

Semantic mappings may be supplied as:
\itemize{
\item named vectors;
\item named lists;
\item two-column data frames.
}

These mappings are normalised internally with
\code{\link[=as_value_key]{as_value_key()}}.

\code{is.prelabelled()} tests if a vector inherits the \code{prelabelled} class.

\code{prelabelled} vectors intentionally separate:
\itemize{
\item observational evidence;
\item semantic operationalisation;
\item contextual semantic refinement.
}

The original observational values remain unchanged while
semantic assertions may evolve through iterative refinement
workflows.

Semantic operationalisation is provided with:
\itemize{
\item \code{\link[=as.character]{as.character()}} for lightweight semantic coercion;
\item \code{\link[=as_character]{as_character()}} for provenance-preserving semantic
workspaces.
}
}
\examples{

x <- c("R","png", "csv", "unknown")

extension_map <- c(
  R = "functional_programming",
  png = "visualisation",
  csv = "tabular_data"
)

x <- prelabel(x, labels = extension_map)

x

is.prelabelled(x)

as.character(x)

semantic_workspace <- as_character(x)

attributes(semantic_workspace)

}
